import { BaseQuestion, Default, Message, When } from './base';
export interface ConfirmQuestion extends BaseQuestion {
    type: 'confirm';
    default?: Default<boolean>;
}
export declare function confirm(name: string, message: Message, options?: {
    when?: When;
    default?: Default<boolean>;
}): ConfirmQuestion;
