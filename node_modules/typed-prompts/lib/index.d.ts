/// <reference types="node" />
import './inquirer';
import { Separator } from 'inquirer';
export * from './questions';
import { Question } from './questions';
export interface PromptFn {
    (questions: Array<Question>): Promise<any>;
    <T>(questions: Array<Question>): Promise<T>;
    (questions: Array<Question>, options: {
        input?: NodeJS.ReadableStream;
        output?: NodeJS.WritableStream;
    }): Promise<any>;
    <T>(questions: Array<Question>, options: {
        input?: NodeJS.ReadableStream;
        output?: NodeJS.WritableStream;
    }): Promise<T>;
}
export declare const prompt: PromptFn;
export declare function separator(useString?: string): Separator;
