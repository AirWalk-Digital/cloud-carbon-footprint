/// <reference types="node" />
declare module 'inquirer' {
    function createPromptModule(options?: {
        input?: NodeJS.ReadableStream;
        output?: NodeJS.WritableStream;
    }): <T>(questions: Array<any>) => Promise<T>;
    class Separator {
        constructor(separator?: string);
    }
}
